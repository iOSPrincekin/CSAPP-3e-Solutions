#include <stdio.h>

/**
 *   考虑下面两个基于IEEE浮点格式的9位浮点表示
 *   1.格式A
 *   有一个符号位。
 *   有k=5个阶码位。阶码偏置是15.
 *   有n=3个小数位。
 *
 *   2.格式B
 *   有一个符号位。
 *   有k=4个阶码为。阶码偏置是7
 *   有n=4个小数位
 *
 *   下面给出了一些格式A表示的位模式，你的任务是把它们转换成最接近的格式B表示的值。
 *   如果需要舍入，你要向+无穷大舍入。另外，给出用格式A和格式B表示的位模式对应的值。要么是
 *   整数(例如17)，要么是小数(例如17/64 或 17 / 2^6)
 *
 * */
int main() {


    printf("Hello, World!\n");
    return 0;
}